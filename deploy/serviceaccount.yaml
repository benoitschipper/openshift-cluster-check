# Apply order: 1 of 5
# This ServiceAccount is used by the health-checker pod.
# No special SCC annotation is needed — the pod runs under the 'restricted' SCC
# (or 'restricted-v2' on OpenShift 4.11+) by default.
#
# Apply with: kubectl apply -f deploy/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: health-checker
  namespace: openshift-health-checker
  labels:
    app: health-checker
